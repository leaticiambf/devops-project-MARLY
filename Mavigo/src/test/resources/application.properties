spring.datasource.url=jdbc:h2:mem:mavigo-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Placeholders requis par application.properties (main)
GOOGLE_CLIENT_ID=test-client-id
GOOGLE_CLIENT_SECRET=test-client-secret
app.secret-key=test-jwt-secret-key-for-testing-only
app.expiration-time=86400000
app.frontend.base-url=http://localhost:5173
app.oauth.redirect-base-url=http://localhost:3000

# Mock OAuth2 for tests
spring.security.oauth2.client.registration.google.client-id=test-client-id
spring.security.oauth2.client.registration.google.client-secret=test-client-secret
spring.security.oauth2.client.registration.google.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.google.redirect-uri=${app.oauth.redirect-base-url}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.google.scope=openid,profile,email,https://www.googleapis.com/auth/tasks

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://openidconnect.googleapis.com/v1/userinfo
spring.security.oauth2.client.provider.google.jwk-set-uri=https://www.googleapis.com/oauth2/v3/certs
spring.security.oauth2.client.provider.google.user-name-attribute=sub

logging.level.org.marly.mavigo.service.stoparea=DEBUG

# Mock PRIM API key for tests
PRIM_API_KEY=test-api-key

# JWT secret for tests
JWT_SECRET=test-jwt-secret-key-for-testing-only-min-32chars
mavigo.jwt.secret=test-jwt-secret-key-for-testing-only-min-32chars
