BACKEND_ORIGIN=http://localhost:8080
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Required for the interactive map in deployed environments.
# This name is kept for compatibility with the current frontend config.
VITE_MAPBOX_TOKEN=
